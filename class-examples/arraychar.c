#include <stdio.h>

// \0 is generally a 'NUL character', or 'sentinel value'. \0 is used in computer memory to separate one string from another,
// because strings can take up a variable number of bytes.
// a string is just an array of characters.

int main(void)
{
    char c[5];

    c[0] = 99
    c[1] = 67

}


